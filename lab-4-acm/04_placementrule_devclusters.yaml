apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: httpd
  name: devclusters
  namespace: simple-app
spec:
  clusterSelector:
    matchLabels:
      environment: dev
